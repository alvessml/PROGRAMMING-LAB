#include <stdio.h>

int main(){
    while(1){
        unsigned char usuario;

        puts("Digite 5 para votar em Paulo e 7 para votar na Renata \n(obs: 0 - voto em branco; Quarquer tecla a mais positivo - voto nulo): ");
        scanf("%", & usuario);

        switch (usuario)
        {
        case :
            /* code */
            break;
        
        default:
            break;
        }
    }

    return 0;
}
