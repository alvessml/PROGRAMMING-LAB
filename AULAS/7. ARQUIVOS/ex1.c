#include <stdio.h>
#include <stdlib.h>

/*  Arquivo - Cáp.9

- Funções mais comuns:
fopen() - abre arquivo
fclose() - fecha arquivo
putc() - escreve caractere em um arqquivo
fput() - O mesmo que putc()
getc() - Lê um caractere de um arquivo
fget() - O mesmo que getc()
fseek() - Posiciona o arquivo em um byte específico
fprintf() - É o para um arquivo o que printf() é para o console
fscanf() - É op ara um arquivo o que scanf() é para o console
fwrite()
fread()
*/

/*Exemplo: */

// FILE *fp;
// if((fp = fopen("teste", "w")) == NULL){
//     printf("arquivo não pode ser aberto\n");
//     exit(1);
// }

// int fclose(FILE *fp); // Quanto retorna 0, é porque foi um sucesso o fechamento.
